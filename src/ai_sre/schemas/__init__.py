"""Pydantic request/response DTOs. One file per resource.

Naming: `<Resource>Request`, `<Resource>Response`, `<Resource>ListResponse`.
These are the wire format. ORM models live in `ai_sre.models` and are
separate by design.
"""
